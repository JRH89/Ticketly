server {
  listen 80;
  location / { root /usr/share/nginx/html; try_files $uri $uri/ /index.html; }
  location /graphql { proxy_pass http://api:8080/graphql; proxy_set_header X-Actor-Id $http_x_actor_id; proxy_set_header Content-Type $content_type; }
}
